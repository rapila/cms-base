MIGRATION Issues for upgrade from Mini-CMS Version 1 > 2 (CMOS)

1. Update and clear caches
2. Clear session (just in case)
3. run migration.sql changes required for update between local version and trunk (version 2)

Migration Issues for previous mini-cms applications
- handle all Blob related changes
	1. $this->oLanguageObject->getData()->getContents() > stream_get_contents($this->oLanguageObject->getData()))
	 	beware that Richtext->parse methods have to be checked individually
- find all Blob, new Blob and migrate them
- FrontendModule::save() > getSaveData
- Test thoroughly before putting online and test again.