ixmos/cmos css work

Browser issues
- list_view css issue in Safari and Chrome, after tab icons that float right, content gets moved down
- FileUpload, if not ready, do not display options
- Compatibility: Firefox (both Mac and Win), Safari + Chrome, Opera?
	add info at login. Do not allow login if not the appropriate browser (min requirements)


General Infos
prepared for skins/themes (jQuery-UI, CMOS)

Special issues
- page_tree
- language_tabs (pages, strings, tips etc)
- accordion tabs (pages, newsletter etc)
- views like thumbnail views, documents, could be some project module or galery module etc

structure
1.
- jquery_ui.css
- jquery-ui.overwrite.css (merge in final version?)
2.
- widget.css ?
  (notififications, buttons?, admin)
3.
- admin.css / preview.css
4.
- context widget.css.tmpl
  (ie admin_menu)

milestones

budget

Richtlinien, Coding convention, naming convention
1. ids only for postitioning of main element (admin), skeleton
2. jQuery ui overwrite