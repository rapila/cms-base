Miscellaneous projects
- Schule Altenburg > server requirements for (windows servers, access rights)
	Synchronisation, shell_exec, Exporte
	install domain on mini-cms and prepare tests, db, temp db, access rights for test with blaccess
	Lehrer, Klassen, Events, Galerie, Werkstücke
- Gems Europe Update HTTP_Request2.php test
	CSS check: Chrome when developer tools are on, the displayed correctly
		Opera main navigation not display correctly
	General check and update
	
PHP 3
- namespaces and coding standards
	http://groups.google.com/group/php-standards/web/php-coding-standard
	http://groups.google.com/group/php-standards/web/psr-0-final-proposal
	
cms_dev, taylormade Bug/issues  
- cms_dev onNavigationItemChildrenRequested second argument not given.
	
Bugs 
- Safari login send form on return does not work
- file upload and drag and drop Safari and Chrome
- double click on active buttons in list (is_protected) does toggle twice but display result of first toggle only

Priorities
- notifyUser search info > replace keys by real values
- discuss permissions @see use_cases_permissions.txt
- validation discuss and sketch (Flash, Propel validator, widget/notify requirements)
- discuss sketch document upload and replace html5 (uploadWidget?), 
	use case, normal, massupload, examples projects (wills, taylormade, mini-cms), galleries
- module, site versioning strategy for continuous development of modules, plugins, example sites (mini-cms, mosaics, taylormade)
  - mini_cms_1, externals etc.
- timeline and herrmanngerman next @see todo_design

PageTypeWidget
- carefully test for reliable behaviour, performance
- adminContainers: if template and pagetype are the same should that be kind of persistant, 
	not rendered all the time (container name and target ol)
- implement/sketch journal page type

PageDetail
- last requested page for leaving pages_admin?
- add loading image for various loading instances
- check all code that I created
- show content & references tabs only if not new
- references, delete restriction, information, load on click or on delete restriction
- templates visualisation, choosing from images rather then names, CSS Parser
- activities show with loader image, ie. save, take away focus from save button
- how to handle language, language objects, permissions etc

PageTree Widget
- new page, move page d&d
- language change and language page strings and objects
- make ajax-loader on loading tree data (ie on has_children icon?) and also on loading page_detail (like lists)
- make separate tree style sheet?
- discuss subtle highlight of active parent elements to make hierarchy explicit
- how to visualize is_folder, how to name it best (Untermenu zeigt)
- make custom made checkboxes. all the same but the active checkbox green/red?

CSSParser, adminGetContainers(), no major priority, can be done anytime
- CSS cleaning for admin containers > why not handle with CSSParser::consumeIfNot(aRules=width, margin, clear, float?)
	Problem > nested containers... how to?
- Caching of result > params page_type and template, modification date temp

Security and Stability!!!!!
- ResourceInclude make stable, fallback for google lib.

Detail
- highlight save button when any input has changed, cancel button should close window if no changes have been entered?
- make save always standout, different then cancel
- discuss tabs, icons-"tabs" for references, list / thumbnail views etc

Widget tasks
- check session storage of widgets WidgetModule::removeStoredWidgets();
- sidebar element edit/delete?
- userNotify > array and links handling for helpful messages (not found)
	remove on click of other element, maybe close button
- detail (row) widget, field widget? @see also project management
- testing QUnit
- how can we simplify and make more transparent and more effective?
- PermissionWidget? @see use_cases_permissions.txt
- d&d, sortable and assign to category or tag etc

SVN, development and tracking tasks
- plugins organise, just change trunk > use new admin ie. newsletter, make new project_management?
- remember > lets continue with tracker and get into it or change it.
- discuss plural classes dir name and new dirs for helper/util, manager, module etc?

RichtextWidget
- tinymce integration continue? @see tinybrowser

Issues	
- blur on filter change
- ModuleManager > group, sort etc, handling for getModuleSelect
- how to include modules in moduleSelect when they are active but not in the reduced moduleSelect array
	this issue concerns permissions and moduleSelect, @see use_cases_permissions.txt
- RichtextUtil 	change names like parseInputFromMce to more generic ones for different editors

html and css
- Css and Skins, discuss how to clean up and make skinable
- check table, ul, heading valid and appropriate elements (heading ul, tbody, thead, tfoot?)
- lang string strategy so I can clean up effectively

dashboard, context help
- if dashboard is a context button I guess it should be in the footer
- help: in detail, list, how to organise ? documentation.mini-cms.ch? how to do?
- think about the requirements of access rights, modules, categories, altenburg etc, discuss Groups / Users / PagesRight

General topics
- content language should always be displayed in tabs, depending on context in pages, strings, wherever?
	does it need a language selector in menu_bar
- context menu > edit sidebar item ie document category etc 
		http://www.javascripttoolbox.com/lib/contextmenu/
		http://beckelman.net/demos/jQueryListViewWithContextMenu/Default.aspx
- avoid multiple loading of same widget
- remove alerts on reload of pages, don't stack the same alert
- remove save_callback: jQuery.noop etc statements
- MiniCMS Redesign DOMAIN and software names @see todo_design.txt

ListWidgets
- cols, solution for align/valign attributes
- delete handling / from list. discuss is_inactive as "temp deleted", delete currently selected category (with documents/documents etc)
- list inline editing, required ? how to implement?
- start and length limit & offset, when and how to use
- add sort field where helpful (documents, and links if category, same for projects? visual sort)

DetailWidget
- open detail widget if requested > users?id=5: current user > maybe show detail widget in current module
	I can activate row but not open widget????
- set_is_loading changed to handle visibility instead of show and hide. loader is not shown anymore, display in dialog titlebar?
- cancel button for convenience added
- save button should change color if content has changed, cancel, warn if changes???
- cancel or password forgotten should be formatted differently than primary action button?
- add default values to login form (password ?)
- handle save exceptions and validation > concept
- open detail widget should be closed on click of sidebar element?
- how to implement References best?
	select (ie document_category) input widgets should be reloaded too on create of sidebar item, if detail is open?
	event listener, observer pattern, needs to be implemented
- delete from detail

SpecialWidgets
- do we need specific row (detail) widgets, inline field in row, or row editing, depends where the event change is?
- do we need field widget? are InputWidgets field widgeds? inline field editing

DocumentDetail
- file upload with jQuery UI? http://www.uploadify.com/demo/
- label_fileupload dynamisch hochladen / ersetzen
- html5, d&d

UsersAdminModule
- is_inactive handling by removing password?

GroupsAdminModule
- add users of this group
- discuss future handling of module rights, ie projects, newsletter, news, maybe documents per category etc.
- discuss handling of users and groups to make it transparent > groups, show users and d&d, users show admin, backend, frontend?
- is inherit required for Pages or should it always be inherit if somebody can edit or create a page, the should do the same for children

LinkDetail
- find more elegant version for protocol options

LinkList
- allow Links to be displayed without category, categories and tags how to?

Validation
- Javascript? how to implement? check required fields in js, rest php

Search options
- How should search be handled - within current criteria, show "more" if category is chosen
- Search Dom elements?
- search words should be handled as separate words, in quotes looking for entire string
- simple search word highlighting?
- indicate sidebar active option that result is only part of selection, on click all are shown again

Login/Logout
	what happens on failed login, default login page with infos?

Admin-Menu
- should not slide in admin
- es kann verschiedene Admin-Menus geben: Preview, Admin, Login, mehr?
- je nach Rechte und Manager werden buttons, selectors, icons zusammenstellen
	- gleiche [logo cmos]
	- verschiedene [modul-selector, edit, preview, user_name, logout, dashboard]
- html / css wie?
- edit / preview button. how does it function in context of pages and admin modules
- selects gruppieren, trennlinien, unterauswahl etc.

LinkAdminModule
- tags in sidebar: discuss handling, drag and drop to tag, d&d in general
- add [without tags] option to sidebar links? general discussion of this
- tags should be able to be removed at once (delete tag_instances and tag if only Link Model is used)
- as soon as tags are shown we have to be aware that links can be tagged more than once. delete options, view all option (see categories)
- categories, the eternal question. Why not global/local categories instead of document categories
- add url validation?
- discuss detail container for states and info?

DocumentAdminModule
- categories in sidebar handling: add [without category] sidebar link
- add document updoad. Make new document uplaod class that works seamlessly from any upload (custom modules, richtext)
- integrate mass upload? seamless functioning of documents
- discuss detail container for states and info?

Categories, type, kind... & tags
- Document categories. How to show options [all, without category]
- design / usability issue: how to choose images only: by kind
- Categories or tags that are shown on pages should display that information.

DocumentCategories
- test link to documents and documents criteria ? bug in results and category highlight?
- add select for category restriction> like image, is_office_doc, video etc. On select show option (ie. max_width, max_height, max_upload_size)
- delete if count > 0? how to handle (action option of deleting all documents related, in documents or in category?)
- show link to related documents
- remove is_inactive....
- should we add created/updated?

DocumentTypes
- is_office_doc: is this needed or deprecated > remove
- need to change anything here?

LanguagesAdminModule
- do inline editing in list since there is not a lot of info
- do allow to add language name string to String from language if language is missing?
- don't allow language to be deleted if default one, or if content already exists, delete language objects?

Propel 1.5 upgrade changes
- discuss migration of Peer methods or new conventions for Query (like always return Criteria instead of result)
- naming conventions?

languages: content_language=language,  interface_language
- handle languages in AdminModule
- refactor code in String Modules or wherever used
- languages selector should only be seen if necessary, probably not in menubar, but in context (tabs) of module (ie Strings, Pages, etc?)
- language_chooser future, language should only be displayed if page is available at language

BackupAdminModule
- should load and backup be in same module, advantage, disadvantage
- add static functions for dashboard hints, warnings

InstallManager and Module config/info and more
- access rights for modules by group, default true
- module info, enabled
- module selector grouping
- custom modules?
- dashboard configuration > special links, backup, notes, email to users

Active / inactive
- reconsider naming of booleans to be intuitivly clear. how to?
- probably documents, links etc should not have a is_inactive because they are not automatically displayed any way. is confusing for users

bugtracking/features issue
- diese todos in den bug-feature tracker? wie das effizient machen?

Misc
- list booleans replace by jQuery-UI icons > active, inactive
- ajax loader implement
- search field test
- + buttons: sidebar and main content ? category / entry
- edit / preview from HerrmannGermann suggestion should be toggle (admin/preview)
- sidebar parameter should be added called from external url: like category or tags or page 
User infos
- create and modify user and date? make accessible? where? info icon?

Category vs tag issue
- tags have to be very clear, how to. 
- "persistent tags" / soft categories...
- question: do people have to know about background technologies?

templates and css
- ui styles, should cmos-button not be ui-button as well
- widget specific, ie. dialog property width / height / min / max
- mark and remove active list state on detail open
- font-size: how to? list vs dialog in Links
- buttons and icons in lists and detail, how to configure: active, is_inactive, is_protected, delete, add, move etc.
- theme to be tuned,
- ???

Performance
- compression and combine...? @see http://www.symfony-project.org/plugins/sfCombinePlugin

CONTINUOUS development strategy
- error handling, ie module instanciaction, display nice error message, no dump
- browser check
	chrome beta does not display confirm alerts properly
- refactoring whenever needed
- wänn öppis "nüd schön isch" > then let's give it a closer look
- performance issues: do we have some
- let's not forget tracker, wiki, context help and info issues
- parallel development of newsletter and client/project module as plugin in new_admin, how to
- what about project management etc see graffle?

Version cleanup
- plug journal > PropelPDO for version _minicms_1.0_plugins
- svn external management how to find out all propedits for site
- what's wrong with gems st?

Sites done remote and 
- taylormade.ch ✓ r2446
- remcom.ch ✓ backup done r2446
- kathleenwills.com ✓ r2446

Sites done locally 
	last update and remote migration.sql check always needs to be done
- svenja.ch ✓
- walo-deuber.ch ✓
- mini-cms.ch ✓
- hubergeologe.ch ✓
- vagabunt.ch ✓
- myplacement.ch ✓
- mosaics.ch ✓

Sites to check something
- summercampsombor.ch ✓
	exclude camp-sites!
- gems-europe.com ✓
	check HTTP/Request2.php and ShopItemImpl
- butti.ch ✓
	check local FLV.php changes, PrettyPhoto
- gcgroup.ch ✓
	what happened to google analytics?	var pageTracker = _gat._getTracker("UA-10691002-1"); pageTracker._trackPageview();
- cgcmac.ch ✓
	newsletter plugin check 
	
Site wait
- mariamalibran.net // dont do anything for now


DONE
*****************************************************************************************************
general issue
- is jQuery or extJS eine Frage, die zu diesem frühen Zeitpunkt gestellt werden müsste?
	das ganze ist unübersichtlich, alles hat vor und nachteile, wir bleiben bei der ursprünglichen idee von jQuery!
- make login work in preview and admin (Firefox does not work)
- don't show content / containers if not logged in admin. discuss the different states
- Bereiche von Admin-Menu mit div elementen: links, mitte, rechts...?
- fix dashboard module selector > dashboard added to selector
- deleteRow() > delete overwrite like reset all document_category_id in related documents > onDelete=setnull
- confirm delete language > funny text node and no {{br}} handling?
- check toggle_active handling, could it be automatic instead of config metadata? > list cell hover handling
- language_input without international
- handle return key for detail save on key.which = 13
- jQuery UI multiple select handling > data document_category_id is string
	mac opera 10.5 does strange stuff> page http://playground/admin/document_types is shown correctly, others not. Windows works, wait for new Mac version
GroupInputWidget
- selected, check and explain widget/select code implemented
- in documents > add category in little Accordion window above plus in infobar
- show document count of related to category in list
- add save keyaccess on return if detail and in focus, save button and return should do the same
- move delegate open() to detail > widget.js ?
- is search configured in ModuleNameListWidget by a method getSearch() (returning modulename or session storage) instead of adding manually in related list widget.js
- add "remove search" icon and function like OS X
- add keyaccess on return if focus on #search_field
- safari sidebar_input select does not work > fixed: added .focus().selected()
- document_kind_input: please check. discuss location (could be in document list table header). When page reloads, storage is gone?
- label "for" handling: why not use id with name and widget number?
- dialog css. formatting alert/confirm and login dialogs. procedure design / jQuery UI theme CMOS....
- selected documents category sidebar element should keep active status even if new category is made
- add Strings to column content. like image, application. Discuss technical vs user ie. swf, flv
- automatic pointer cursor if toggle_boolean is set? implemented a manual version by adding toggle_active param to metadata
- add span to sort header so icon is next to header text
- lists hightlight sidebar item when ie documents?document_category_id=1, users?group_id=1 is called
	ckeck logic (ie. DocumentAdminModule)
- criteria > no entries/not found > show meaningful information
- is_sortable by joined tables, ie document kind document type:
- sort updated_at_formatted by fieldname updated_at?
- sort user fullname by first_name, last_name?
- lists how to add icons depending on property like extension or document_kind
- ajax_loader
- fix dialog width option. does not work yet
- alert and login css, add class to overwrite special dialog header and footer etc?
- Listwidgets, row header anzeigen auch wenn kein Datensatz @see reload


Zusammenfassung Treffen HerrmannGermann
- Ankommen beim Baum? Wozu braucht es ein Dashboard. Nur machen wenn es Sinn macht und genaue einfach erstellbare Inhalte hat.
- Kontexthilfe, wie Hilfe machen, mini-cms Module mit Hilfe für Module, Detail, Sidebar und Actions, Webservices? Screenvideos!
- Auswahlmenu entschlacken und gruppieren, eventuell subselects (admin > types, categories, what else)
- [alle anzeigen], ohne Kategorie, Tags?
- Linktags analog Kategorien implementieren
- Doppelklick öffen, einmal für d&d, wie im Sidebar? Alles sollte klar funktionieren
- netzwerkverbindung verloren alert
- detail view abbrechen
- anzahl 5/5 wie gemeint, implementieren wenn nötig
- Allgemein. Neudesign ist für Dummies. (moderne features können trotzdem funktionieren, fn keys, filter?)

- implement default sidebar element all, maybe without category
- wills Export error: '[Output of /usr/bin/mysqldump:
mysqldump: Got error: 1044: Access denied for user 'cp23810_kwillac'@'localhost' to database 'database' when selecting the database.(2 === 0 ? '' : '; I am '.exec('whoami')]'
- taylormade environment development does not work, check trunk, version-1-devel
	Settings::getSetting() in inc.php does not work, resource finder?
- mini_cms_scripts clear_cache, does it need modifications to work with both Propel 1.2 and 1.5 (), @see PHP Parse error:  
	syntax error, unexpected T_PAAMAYIM_NEKUDOTAYIM in /Users/jmg/Sites/playground/base/lib/classes/Module.php on line 42
	I added old path_to_propel_generator to generate script
- dblclick is not very handy.
  why no leave row click for edit and add move/change category icon with special event handling?
	added edit "pencil" and moving "?" icons to document_list
- PagesAdminModule, Preview, addContent ie list documents by category and test inline features
- getCustomListElements(), highlight default option using addResourceParameter to provide "initial_selection"
	check and confirm handling, simplify or abstract if possible since it is a recurring behaviour
	fix default select_all highlight on toggle active element and don't show pointer/trigger reload if select_all is selected and highlighted
	add classnames to custom (all, default) elements, handle ie address book kind of icons to sidebar elements
- Kategorien und Gruppen, sidebar heading, for transparency should be same like list headings
  @see list widget.js line 93 fix todo, should be no li element, refactor code if any heading should be required
- implement temporary tag for identical "category" handling in links or whatever
- replace hard coded "Hinweis:" in new_admin.js by string
- check row_count_info reload
- usersgroup can show user twice or more, if user group is selected, only group should be removed. How to visualise, make clear.
	discuss these handling issues for categories, tags, groups etc, hard and soft categories
- widget instances if edit & new then 2 otherwise one?
- exception on save should be notified in detail widget 
- make dialog with tabs for groups or whatever? test and prepare for design session
- if is admin -> add edit etc groups from users, make it easy?
- user group storage doesn not work proparly
- what
- save handling of created and updated? with Propel 1.5? @see User::save()
- I get user_list widget and set and getUserKind by using get/setOption of list widget. That makes it consistent, but maybe also complicated
- icon / and has_data???, display icon in user_kind row ui-icon-true???
- is_backend_login_enabled error see email CriteriaListWidgetDelegate::__call() exception missing getIsBackendLoginEnabled()
- call setUserKind, getUserKind on UserListWidget to simplify?

Widget handling : generell und Beispiel heading_filter
- widget content on the fly erstellen > instanciateWidget
- Widget.createWithElement() ?
- egal ob das widget ein input oder ckeckbox oder wasauchimmer ist
- input-widgets defaultmässig event fire() und handle() bei geändertem inhalt (input.value_changed) (nicht: form > onchange form.submit)
- PHP vs Javascript ?
- erst beim durchdenken kommt der ganze scheiss (Zitat Rafi). Denken wirs durch!
- sidebar selector? groups, is_backend_login_enabled, is_admin, is not backend_enabled > heading_filter????
bug 
- 'Error in Cache->__construct(): Cache folder generated/caches/images does not exist'
	Caches dir with subdirs should be in repository or created if not exist
- UserListWidgetModule > user_group == null criteria
-	User->getUserRelatedByCreatedBy(), User->getUserRelatedByUpdatedBy() for user detail

Criteria and Model
- propel_additions
ErrorHandling, Debug, Autoload issues
- how can we make Autoloader recognise changes?
- how can we prevent problems created by old session
- how can we make ErrorHandling display dumps effectively in "all" cases, before header is sent?
- DocumentTypePeer::getDocumentKindsAssoc(), wo kommt der String Dokumente / Andere her????? Hilfe. war in backend/documents/lang!
-	site/lib/vendor/FLV/FLV.php , is external
release-1-devel propel update
- DefaultPageTypeModule::fillContainerWithModule > handle $oContentObject->getConditionSerialized() @see cms_cgc
- JournalEntry::getCreatedAtTimestamp() missing, since behaviours or attributables are not generated in release-1-devel, how to resolve this
- $oConnection->query() in MysqlDumpLoaderBackendModule.php on line 103
- Blob understanding $oModule->save(new Blob (oLanguageObject))
- handle Richttext blobs in general more issues: don't change for easier migration of release-1-devel sites? confusing
- DocumentUtil::updateDocument() ??
- check all instances of getContentInfo() like MediaObjectFrontendModule
- check FormFileModule $this->oFormStorage = unserialize(stream_get_contents($oFormDataLanguageObject->getData()));
- changed: parseStorageForOutput($sStorage) if $sStorage is_resource > stream_get_contents
- changed: CheckBackendModule RichtextUtil::parseStorageForBackendOutput(stream_get_contents($oLanguageObject->getData()));
	doCheckContent() > $oLanguageObject->setData($oRichtextUtil->parseInputFromMce());
	discuss > see JournalEntry behaviour branch handling
- remove MigrationBackendModule or modify it if can be used
- integrate new rs http download to MysqlDumpLoaderBackendModule::doToDownload
- FilterCriteria add to CriteriaListWidgetDelegate
	use new Query features or not
-	event listening > how to update input widget (ie detail document_category on new/change of category in sidebar)
- check hierarchy of js loading > bug Chrome initial section highlight
- set default value for filter columns
- fix header_filter widget (file_kind, user_kind etc)
- use widget (widget.document_kind...) in list widget header
- Propel install for parallel old version maintenance and new development including dependencies?
- hover: on page li hover the children should not be highlighted, they should have their own color?
- nested li, the last child should have no line and no bottom padding (now the parent li is handled like this)
- tree view? http://jquery.bassistance.de/treeview/demo/
- bug user detail js
- PageDetail save, delete event handlers, was müssen wir langsam beachten wegen Preview
- Template getCustomPageProperties how to
- Load children on click of parent when children are not open
- user_list widget error, failed to debug it
- open_item on click of page with children that are not loaded yet
- how and where to handle page properties. should be configurable (off in most cases, too many notes, if not used)
	show page_property options only only if configured, how to do that with JSON the most efficient way
	unter Template anzeigen > Einstellungen/Properties, dieses Wort nur anzeigen wenn Properties aktiv.
	Dies soll keine Einschränkung von usern sein, sondern nur Ansichten entschlacken und übersichtlicher machen.
	im "Dashboard" kann es den Hinweis geben, wie man das freischaltet.
	Auswahl der vorhandenen PageProperties anzeigen. Template->getCustomPageProperties() with name and default value
- best way to add use-folder-icons class to sidebar widget? setListTag() also
- initial_selection does not work in sting_list/document_type_list, both no id
- tree widget
- test tree widget
- pages list should be also full list with visible status for overview?
- nested set vs hierarchical query http://en.wikipedia.org/wiki/Hierarchical_query
-	page detail should not be a pop-up widget, should be full content? different visualisation @see design?
- page insert (input) widget?	new page handling with + or on right click
- is_inactive on page_string level
- page tree with accordion, config open tree and/or toggle (small sites default open)
- Chrome select does not work (sometimes)
- Page does not show in Firefox without Firebug activ, or ErrorHandler::log(), In other Browsers it does! > console.log()!
- bug 'Undefined variable: ret', [filename] => '/Users/jmg/Sites/playground/base/lib/vendor/propel/collection/PropelObjectCollection.php'
	bug fixed by checking Page->hasChildren, otherwise return array in PagesAdminModule::listChildren()
- page_id = usePath if exists else root_page_id, open corresponding page branch containing page and highlight
- Transmit d&d rights writing dump
- http://www.triboje.com/ ?? united domains
- buttons in infobar, how to add, style and position them best?
- noch einmal Blob! devel und trunk z.B. FrontendModule::getData();oLanguageObject->getData & stream_get_contents()??? check
- page detail save on return/enter > bind(keydown) { if(event.which === 13) > page detail save }
- template, page_type, containers and objects > language_objects
- Template und Module, Einstellungen, Wie kann man das Ganze übersichtlich machen?
- how to visualize templates most effectively????
- fill only containers at page_load and make method ->getContainerObjects(); 
- move containers_content to it's own method widget::content_load(container_name)
- are DetailWidgets a delegate of a PageTypeWidget or viceversa
- Keyword "default" error on Chrome, Opera etc. Widget.types.page_type.types.default, how to solve? name change to "standard"
- sort order of objects does not work properly yet, looks like mysql lock... problem, if ErrorHandler::log() > correct
- fix accordion, only one should be open. page_properties should not be visible by default
- lang strings admin strings should overwrite backend strings, or ignore backend strings for new_admin
- highlight initially selected tree element, like root_page of usePath Page
- open initially selected item all the time if has childen
- update tree element on save
- TODO: re-setting the last selected item after reload should be default functionality
- fix select menu selected
- strategy keywords/namespaces browsers, ie page_type "default" (unexpected token default Chrome)
	keyword delete: SOLUTION:page_detail.delete() renamed to deletePage() since it throws exceptions in all browsers but Firefox
- Bug update_row on save in ContributorDetailWidgetModule > line is removed because of different saveData return value?
	if(setRoleBitmap is executed, then the row is deleted if the bitmap changes)
-	CriteriaListWidgetDelegate::FILTER_TYPE_BIT_IS_SET, debug cmos_mini contributor sidebar select if multibit
- Contributor::countProjectsRelatedByClientId() does not work ?
- mini-cms_generate > schema.sql > migration.sql add table link_categories Propel Generator Error?
- getListContent Limit arguments consistency
	array_splice($aResult, $iRowStart, $iRowCount) does not work when custom elements are manually merged
- DumpExporter wirft mit FireBug on eine JS exception, führt den Export bei neuladen der seite jedoch aus  Lösung, Prototype?
- check detail widget changes fill_select
- Opera (or more) Error in ResourceIncluder->addJavaScriptLibrary(): Library version=1.4 not found > update to 10.54 seems to have solved the issue
- usedWidgets(), what does it do, and is it necessary ? yes, it preloads all widget.js and that's good
- Invalid argument supplied for foreach() in /Users/jmg/Sites/cmos_mini/base/lib/classes/Settings.php on line 22 > YAML 1.0 declaration removed
- add page_string attributes to tree > linktext language, active check both on page and page_string!
- Opera and Chrome do not load sidebar tree and click on sidebar elements don't work
triboje.com
media object : get_headers() [<a href='function.get-headers'>function.get-headers</a>]: URL file-access is disabled in the server configuration
- add active checkbox to label??? page_string? design
- Firefox > cmos_mini/admin/users search no_result error, shows entries but notifies no result, relates to no sidebar elements?
	there is no notify if one console.log(this, list); in list widget > completeList() > NOTE > fixed with pastEvents for sidebar and main_area
- error in cmos_mini LocalJsLibraryFileModule, whatdoes it mean
	 ([code] => 2, [message] => 'file_get_contents(): php_network_getaddresses: getaddrinfo failed: nodename nor servname provided, or not known', [filename] => '/Users/jmg/Sites/cmos_mini/base/modules/file/local_js_library/LocalJsLibraryFileModule.php', [line] => 25, [context] => ([oCache] => Cache Object: ([bCacheIsNeverOff] => false, [sFilePath] => '/Users/jmg/Sites/cmos_mini/generated/caches/templates/53e3d2bea2fb858a7e0de9a44c0a43f0.cache', [bCacheControlHeaderSent] => true), [oIncluder] => ResourceIncluder Object: ([aIncludedResources] => ([-1] => (), [0] => ([lib_jqueryui] => [...]), [1] => ()), [aCurrentDependencyStack] => ()), [sLibraryVersion] => '1', [bUseCompression] => false, [bUseSsl] => false, [sContents] => '', [sLocation] => 'http://ajax.googleapis.com/ajax/libs/jqueryui/1/jquery-ui.js'), [referrer] => 'http://cmos_mini/admin/users', [host] => 'cmos_mini', [path] => 'local_js_library/jqueryui'), referer: http://cmos_mini/admin/users
	NOTE > php fopen wrappers disabled
- user groups are not stored anymore in user > FIXED > js 'group_ids[]' translates into PHP $_REQUEST['group_ids]
- change names of DefaultPageTypeModule admin methods sortObjects : adminSortObjects, removeObject > adminRemoveObjects
- delete, cancel/reset and save buttons should only be visible when ready
- display linktext in page tree (on hover page_title)
- Chrome does not show infobar buttons > callbacks called only when widget prepared
- css page tree li ui-state-active: if parent element is hovered, the active background turns transparent! >set color for active element to !important
- taylormade cufon.now() slashes?
