Pages

detail: 
- Content Language Issue:
	- add language chooser? 
		for changing main content language: for all language sensitive modules (pages, strings, others)
	- output of page_tree: Text instead of Name
- Accordion tabs suggested HerrmannGermann DISCUSS
	- Beschreibung (Inhalt)
	- Template (Vorlage) und Seitentyp
	- Konfiguration
- Configuration 
- handle active accordion intelligently	
- References: how to handle. save notifications easy links
- preview / publish handling, what about a light version with data_preview blob?

configuration
- fix bug (missing language_object or blob null?), handle objects without language_objects
- Display content_info if exists?
- allowed_modules are not handled anymore
- CssParser DISCUSS: use only if works properly. SIMPLE option for now: configuration.css
	Problem > nested containers and more
- BackendSave for FrontendModules: make clear naming convention, allow tracking in modules!!!! @see document_categories/link_categories

language sensitive: page_strings, description, configuration, references!
- templates visualisation, choosing from images rather then names, CSS Parser
- visualise activities, whenever something is loaded, ie on tab?
- page preview before set online, how to do?
- make custom made checkboxes. all the same but the active checkbox green/red?

page_types:
- implement other page types for testing and improving page type handling
- handle inherit, display empty only if inherit is set, change name: stop inheritance
	
tree:
- new page add in tree (if no major hazzle)
- drag&drop
- @see Content Language Issue:
- page_string:isActive has to be visualised in the tree (not page:isActive)
	PageString::isActive should have fallback (if isActive, check Page::isInactive==false)
	
last_edit_page:
- what happens when module category is chosen that is used in a page, should it effect last edit page/preview


