MIGRATION Issues for upgrade from Mini-CMS Version 1 > 2 (CMOS)

1. Update and clear caches
2. Clear session (just in case)
3. run migration.sql changes required for update between local version and trunk (version 2)
4. make release-1-devel MIGRATION changes if older than r2442

PageTree changes
- remove parent_id, sort (after mini_cms_migrate_adjacency_list_to_nested_set.sh, @see migration.sql)
- replace Page/PageString::getLongTitle(), identifiers 'long_title' by getPageTitle(), 'page_title'
- replace Pape/PageString::getTitle(), idendifiers 'title' ??? watch out, getLinkText(), getLinkTextOnly()
- getTree() is deprecated
