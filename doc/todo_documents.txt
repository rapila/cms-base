Documents
detail
- fix file_upload widget_type bug!
	widgetInformation: undefined??
- add_item/new,replace image: prevent enter to send form, klick upload button
- return on detail with file_upload replace should not close window and show new uploaded file
- upload activity should be shown in detail window.

list / views
- clean up views, make thumbnail view work in the same way (same criteria delegate)
- sort column: visible only if category is chosen, AdminModule::customCss()
- fix row_count in infobar
- naming convention, functionality and hierarchy of widgets with views, @see documents
	document_thumbnail_view.js can easily be abstracted for general thumbnail views?
- edit link and sort should function
- document_list thumbnail_view AUTOPLAY FALSE
- double click on is_protected (i.e) in list does not display the result correctly (stores correctly, but not displays correctly)
	handle with timeout? so row can be updated

document_types
- allow add new document type on upload if has permissions
	validate document_type @see todo_user_notifications > exception display error at/around file_info / browse? DISCUSS
- IMPORT doc types if new? automatically or dialog import or manage

drag & drop 
- multiple upload: show activity_symbol (big) in center of page AND if possible filename changeing
	fix display select document_category 
- list: drag sort, highlight top line of hovered list item, last one add dummy list item?
- category change: highlight category like file upload

sidebar: document_categories:
- is_office_doc: is this needed or deprecated > remove
- add field to restrict to images only
- make max_width param handling more simple: show special config on click only, 
	display select for (max_size, max_width, max_dimension)
	max_upload_size?
- question: edit category in sidebar should display modal dialog?
- IMPORTANT: for user info, sidebar categories should be highlighted if published on web (languague_objects)

RELATED projects
- Mini-CMS Version 2 projects file_upload



