Strings

detail
- change string_key behaviour should be transparent (previous string object is "deleted")
- create new: sidebar_changed fix

list
- discuss separate columns for each language with info, great: link edit and open with active language tab

dashboard:
- ie. pages: display all page related infos, including meta texts > link to meta texts, so they can be edited directly
	(initial namespace and request string id)